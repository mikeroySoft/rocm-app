{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the main window. Grants no shell execution, no filesystem access, and no arbitrary HTTP. Every privileged operation goes through a typed command in this crate, never a generic plugin permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
